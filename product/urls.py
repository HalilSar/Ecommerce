from django.urls import path
from . import views



urlpatterns = [
    # path('', views.index, name='index'),
    # path('detail/<int:product_id>', views.GetById, name= 'detail'),
    # path('category/<int:category_id>/<slug:slug>', views.GetByCategoryId, name= 'category'),
]

    